# Copy this file to .env.local and fill in your values

# AWS Configuration
# You can also use AWS CLI profiles instead of these environment variables
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here

# AWS Region (required for Terraform and deployment)
AWS_REGION=us-west-2

# CloudFront Distribution ID (auto-populated by update-env-from-terraform.sh)
# CLOUDFRONT_DISTRIBUTION_ID=E1234567890ABC

# S3 Bucket Name (auto-populated by update-env-from-terraform.sh)
# S3_BUCKET_NAME=jmoyers.org

# CloudFront Domain Name (auto-populated by update-env-from-terraform.sh)
# CLOUDFRONT_DOMAIN_NAME=d123456789.cloudfront.net

# ACM Certificate ARN (auto-populated by update-env-from-terraform.sh)
# CERTIFICATE_ARN=arn:aws:acm:us-east-1:123456789:certificate/abc-123

# Route53 Zone ID (auto-populated if using Route53)
# ROUTE53_ZONE_ID=Z123456789ABC

# Optional: AWS Profile to use (alternative to access keys)
# AWS_PROFILE=your-aws-profile-name

# Domain configuration (should match terraform variables)
DOMAIN_NAME=jmoyers.org

# Advanced configuration (optional)
# CLOUDFRONT_PRICE_CLASS=PriceClass_100
# PROJECT_NAME=jmoyers-org
# ENVIRONMENT=production
# ENABLE_VERSIONING=true
# DEFAULT_CACHE_TTL=3600
# STATIC_CACHE_TTL=31536000 